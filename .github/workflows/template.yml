name: Clone Repository from Custom Template

on:
  repository_dispatch:
    types:
      - template

jobs:
  clone_repository:
    runs-on: ubuntu-latest

    steps:
      - name: Clone Custom Template Repository
        run: |
          git clone https://github.com/georgiesamaha/testTemplate .
          rm -rf .git
          git init
          git add .
          git commit -m "Clone custom template repository"
          git remote add origin "https://github.com/$GITHUB_REPOSITORY"
          git push -u origin main
